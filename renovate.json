{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "group:expoMonorepo", "helpers:pinGitHubActionDigests"],
  "dependencyDashboard": true,
  "schedule": ["before 6am on monday"],
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "An Expo SDK bump has to move every expo package together via `npx expo install --fix`, then be rebuilt and tested on a device. Approve these by hand from the dependency dashboard.",
      "matchPackageNames": [
        "expo",
        "expo-*",
        "@expo/*",
        "react",
        "react-dom",
        "react-native",
        "react-native-*",
        "@types/react",
        "jest-expo"
      ],
      "dependencyDashboardApproval": true
    },
    {
      "description": "Gradle dependencies come from Expo's prebuild output and changing them needs a native rebuild plus an on-device test, so they wait for approval too.",
      "matchManagers": ["gradle", "gradle-wrapper"],
      "dependencyDashboardApproval": true
    },
    {
      "description": "Every other major version waits for a deliberate decision rather than arriving as a pull request.",
      "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true
    }
  ]
}

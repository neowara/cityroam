<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!-- Home-screen widget that mirrors the live trip module + board telemetry.
         Rendered by the launcher from RemoteViews built in CityroamWidgetProvider;
         the JS side pushes a compact snapshot into SharedPreferences via
         CityroamWidgetModule.updateSnapshot, and this provider reads it back. -->
    <!-- The receiver must be exported so the launcher (a separate system app) can send it
         the APPWIDGET_UPDATE broadcast and bind to it; an unexported provider never shows
         up in the home screen's widget picker. -->
    <receiver
      android:name=".CityroamWidgetProvider"
      android:exported="true"
      android:label="@string/cityroam_widget_label">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/cityroam_widget_info" />
    </receiver>
  </application>
</manifest>

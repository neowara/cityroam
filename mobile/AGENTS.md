# Notes for coding agents in mobile/

- This is Expo SDK 57. Read the versioned docs at https://docs.expo.dev/versions/latest/
  before writing code; APIs have changed between SDKs.
- Never run `eas build` without `--local`. Release builds are local only (see README).
- Repo-wide agent notes are in [`../AGENTS.md`](../AGENTS.md).
